openapi: 3.0.0
info:
  title: W03 CRUD API
  version: 1.0.0

servers:
  - url: https://proyectcrud.onrender.com
    description: API desplegada en Render

paths:
  /api/products:
    get:
      summary: List products
      responses:
        '200':
          description: Success
  /api/orders:
    get:
      summary: List orders
      responses:
        '200':
          description: Success
